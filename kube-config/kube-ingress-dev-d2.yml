apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: aspdotnet12
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  tls:
  - hosts:
    - dev-d2.co
    secretName: tls-secret
  rules:
  - host: dev-d2.co
    http:
      paths:
      - path: /
        backend:
          serviceName: aspdotnet12
          servicePort: 9081
      - path: /*
        backend:
          serviceName: aspdotnet12
          servicePort: 9081